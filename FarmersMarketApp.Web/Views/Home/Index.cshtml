@using FarmersMarketApp.Web.ViewModels.ProductViewModels
@model IEnumerable<AddProductFarmOptions>
@{
	ViewData["Title"] = "Home page";
}

<div class="row justify-content-center">
	<div class="text-center col-8">

		<p class="mt-2 mb-4">
			Welcome to The Jolly Farmer's Market, where the veggies are fresh,
			the smiles are fresher, and the only thing serious around here
			is our love for food! We’re your one-stop spot for carrots that crunch
			louder than your morning alarm and tomatoes so ripe, they might just
			convince you they're fruit (again).
		</p>

		<p class="mt-2 mb-4">
			Take a scroll through our carousel and feast your eyes on the bounty
			of farm-fresh goodness: vibrant greens, juicy reds, and the occasional
			oddly-shaped squash that’s basically a conversation starter. Above all,
			we believe your shopping should come with a side of laughter — because
			nothing pairs better with produce than a good chuckle.
		</p>

		<div id="carouselExampleCaptions" class="carousel slide">
			<div class="carousel-indicators">
				<button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="0" class="active" aria-current="true" aria-label="Slide 1"></button>
				<button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="1" aria-label="Slide 2"></button>
				<button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="2" aria-label="Slide 3"></button>
			</div>
			<div class="carousel-inner" style="max-height:600px;">
				<div class="carousel-item active">
					<img src="@Model.First().ImageUrl" class="d-block w-100" style="width:100%; max-height:600px; object-fit:cover;" alt="@Model.First().Name">
					<div class="carousel-caption d-none d-md-block">
						<a asp-controller="Product" asp-action="All" asp-route-farmId="@Model.First().Id" style="text-decoration: none;">
							<h5>@Model.First().Name</h5>
							<p>@Model.First().City</p>
						</a>
					</div>
				</div>
				<div class="carousel-item">
					<img src="@Model.Skip(1).First().ImageUrl" class="d-block w-100" style="width:100%; max-height:600px; object-fit:cover;" alt="@Model.Skip(1).First().Name">
					<div class="carousel-caption d-none d-md-block">
						<a asp-controller="Product" asp-action="All" asp-route-farmId="@Model.Skip(1).First().Id" style="text-decoration: none;">
							<h5>@Model.Skip(1).First().Name</h5>
							<p>@Model.Skip(1).First().City</p>
						</a>
					</div>
				</div>
				<div class="carousel-item">
					<img src="@Model.Last().ImageUrl" class="d-block w-100" style="width:100%; max-height:600px; object-fit:cover;" alt="@Model.Last().Name">
					<div class="carousel-caption d-none d-md-block">
						<a asp-controller="Product" asp-action="All" asp-route-farmId="@Model.Last().Id" style="text-decoration: none;">
							<h5>@Model.Last().Name</h5>
							<p>@Model.Last().City</p>
						</a>
					</div>
				</div>
			</div>
			<button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide="prev">
				<span class="carousel-control-prev-icon" aria-hidden="true"></span>
				<span class="visually-hidden">Previous</span>
			</button>
			<button class="carousel-control-next" type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide="next">
				<span class="carousel-control-next-icon" aria-hidden="true"></span>
				<span class="visually-hidden">Next</span>
			</button>
		</div>

		<p class="mt-4 mb-4">
			So, come on down (or scroll on up)! Whether you're here for the perfect
			pear or just browsing for that picture-worthy pumpkin, we’re here to make
			sure your grocery trip is a-maize-ing. 🌽
		</p>
	</div>
</div>
